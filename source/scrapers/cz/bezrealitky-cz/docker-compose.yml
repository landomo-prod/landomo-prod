version: '3.8'

services:
  bezrealitky-scraper:
    build:
      context: ../../..
      dockerfile: scrapers/Czech Republic/bezrealitky/Dockerfile
    container_name: bezrealitky-scraper
    restart: unless-stopped
    ports:
      - "8082:8082"
    env_file:
      - .env.docker
    networks:
      - landomo-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8082/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - "scraper.name=bezrealitky"
      - "scraper.country=czech-republic"
      - "scraper.category=primary"

networks:
  landomo-network:
    external: true
