/**
 * Sample Property Data
 *
 * DEPRECATED: This file is kept for backward compatibility.
 * Use @/lib/properties-mock instead for new code.
 *
 * The data now uses the backend-compatible format (PropertyResult)
 * and is automatically adapted to the frontend Property type.
 */

// Re-export from the new mock file
export {
  sampleProperties,
  getPropertyById,
  getFeaturedProperty,
  filterProperties,
} from './properties-mock';
